<UserControl x:Class="SQLite.WPF.Views.Tabs.TableChartTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:chart="clr-namespace:Utility.WPF.Chart;assembly=Utility.WPF.Chart"
             xmlns:vm="clr-namespace:SQLite.ViewModel;assembly=SQLite.ViewModel"
             d:DataContext="{d:DesignInstance  vm:TableChartViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        <chart:ColumnSelectionsGrid x:Name="ColumnSelectionsGrid"
                                    ColumnDetails="{Binding ColumnData}"
                                    ColumnPropertyKey="Name"
                                    ColumnEnabledKey="IsEnabled"
                                    ColumnSelections="{Binding ColumnSelections,Mode=TwoWay}"/>

        <chart:DynamicSelectionChart Grid.Row="1" x:Name="DynamicChart" Series="{Binding Series}"/>
    </Grid>

</UserControl>
