<UserControl x:Class="Utility.WPF.UserControls.MainWindow.StatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Utility.ViewModel;assembly=Utility.ViewModel"
             d:DataContext="{d:DesignInstance vm:StatusViewModel}"
             xmlns:local="clr-namespace:Utility.WPF.UserControls.MainWindow"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <StatusBar >
        <StatusBarItem>
            <TextBlock Text="{Binding StatusInfo}" FontWeight="DemiBold"
                           Foreground="{DynamicResource ApplicationFontColor}"/>
        </StatusBarItem>
    </StatusBar>
</UserControl>
